general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

dependencies:
  pre:
    - rm -Rf node_modules

deployment:
  staging:
    branch: master2
    commands:
      - npm run build
      - ./scripts/deploy-ghpages.sh dist

machine:
  node:
    version: v7.0.0
