general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

dependencies:
  pre:
    - rm -Rf node_modules
    - npm install
    - npm run build

deployment:
  staging:
    branch: master
    commands:
      - ./scripts/deploy-ghpages.sh dist
