general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

dependencies:
  pre:
    - rm -Rf node_modules

deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - git config --global user.email "eerrecalde@hotmail.com"
      - git config --global user.name "Emiliano User Machine"
      - ./scripts/deploy-ghpages.sh dist

machine:
  node:
    version: v7.0.0
